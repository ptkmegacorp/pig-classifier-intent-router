Keep the current pipeline unchanged.
Do not rewrite the whole thing.

Generalize CommandIR from fixed unions to string-based family/action/object/target/slots.
Move valid command schemas into compiler.json.
The center should validate against metadata, not against TypeScript unions.
Turn state/preconditions into a provider registry.
No more adding every new context fact to one switch statement forever.
Cache embedding docs.

Use embeddings as optional semantic recall, not as the main control plane yet.
Make BM25 + metadata the default source of truth.

the pig extension main goal and intent is to be a: natural language to unix style computer control compiler. 

so we want less hardcoding. 
